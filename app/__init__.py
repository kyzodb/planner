"""The planner MCP server program (lives in `mcp/`): one server, one route file over the domain — the
work board (`api/board.py`) — mounted on the one FastMCP instance `api/__init__.py`
constructs."""
