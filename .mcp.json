{
  "mcpServers": {
    "board": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}",
        "python",
        "${CLAUDE_PLUGIN_ROOT}/app/main.py"
      ],
      "env": {
        "PLANNER_REPO": "${CLAUDE_PROJECT_DIR}",
        "PLANNER_BOARD_OWNER": "${user_config.board_owner}",
        "PLANNER_BOARD_REPO": "${user_config.board_repo}",
        "PLANNER_BOARD_PROJECT": "${user_config.board_project}",
        "UV_PROJECT_ENVIRONMENT": "${CLAUDE_PLUGIN_DATA}/venv"
      }
    }
  }
}
